create or replace TYPE contratosobj as object (

	id number, 
	id_empleado number,
	fecha_inicio Date,
	fecha_fin Date,
	salario number,
	por_com number
);

create or replace TYPE BODY contratosobj as
	
END;